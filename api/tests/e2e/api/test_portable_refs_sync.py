# Portable refs sync feature was removed as part of workspace redesign.
# Entity files are now stored in entity tables (workflows, forms, agents),
# not in workspace_files. GitHub sync no longer uses portable refs.
